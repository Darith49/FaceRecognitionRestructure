import 'package:flutter/material.dart';

class MytemScreen extends StatelessWidget {
  const new({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My Team Screen'));
  }
}
